interface:
  display_name: "NEEP Vocab"
  short_description: "Local NEEP word lookup and search"
  default_prompt: "Use $neep-vocab to check whether words are in the local NEEP list or search the lexicon."
